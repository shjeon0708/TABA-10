sudo dnf install -y rpm-build make gcc readline-devel mock

sudo dnf install -y \
  autoconf automake libtool \
  perl-generators python3-rpm-macros

# rpmbuild 디렉터리 준비
mkdir -p ~/rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
echo '%_topdir %(echo $HOME)/rpmbuild' > ~/.rpmmacros

# SRPM 설치
rpm -ivh rlwrap-0.46.2-2.el9.src.rpm

# 빌드
rpmbuild -ba ~/rpmbuild/SPECS/rlwrap.spec

sudo dnf install -y ~/rpmbuild/RPMS/x86_64/rlwrap-0.46.2-*.rpm
rlwrap --version